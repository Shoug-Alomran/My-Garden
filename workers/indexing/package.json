{
  "name": "shoug-indexing",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "python3 ../../scripts/build_indexing_manifest.py",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
